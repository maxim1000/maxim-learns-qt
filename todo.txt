connect first and last when mouse is outside of the design area (color is the same as for other dashed lines)
make a better frame for the design area
make a more convenient window size at the start
avoid polygon reallocating on every mouse move
